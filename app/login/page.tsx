'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {LogIn,ShieldCheck,Loader2} from 'lucide-react'; import {login} from '../../lib/api';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('admin@irkop.local');const [password,setPassword]=useState('demo1234');const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);const [err,setErr]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setErr('');try{await login(email,password);r.replace('/dashboard')}catch(e:any){setErr(e.message||'Login gagal')}finally{setBusy(false)}}return <div className="mx-auto grid min-h-[calc(100vh-7rem)] max-w-5xl place-items-center"><div className="grid w-full max-w-4xl overflow-hidden rounded-3xl border bg-white shadow-soft md:grid-cols-2"><div className="hidden bg-slate-950 p-10 text-white md:block"><div className="grid h-11 w-11 place-items-center rounded-xl bg-teal-500 font-black">I</div><h1 className="mt-8 text-3xl font-bold">IRKOP CELL</h1><p className="mt-3 text-sm leading-6 text-slate-400">Dashboard & Kasir terhubung ke Central API + D1.</p><div className="mt-10 space-y-3 text-xs text-slate-300"><div className="flex items-center gap-2"><ShieldCheck size={15} className="text-teal-400"/>JWT authentication</div><div className="flex items-center gap-2"><ShieldCheck size={15} className="text-teal-400"/>RBAC server-side</div><div className="flex items-center gap-2"><ShieldCheck size={15} className="text-teal-400"/>Data real dari D1</div></div></div><div className="p-7 sm:p-10"><h2 className="text-2xl font-bold">Masuk</h2><p className="mt-1 text-sm text-slate-500">Gunakan akun yang terdaftar di backend Konter.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-xs font-semibold">Email<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border p-3 text-sm outline-none focus:border-teal-500" autoComplete="username"/></label><label className="block text-xs font-semibold">Password<div className="relative mt-2"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} className="w-full rounded-xl border p-3 pr-20 text-sm outline-none focus:border-teal-500" autoComplete="current-password"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-2.5 text-xs text-slate-500">{show?'Sembunyikan':'Lihat'}</button></div></label>{err&&<div className="rounded-xl bg-rose-50 p-3 text-xs text-rose-700">{err}</div>}<button disabled={busy} className="flex w-full items-center justify-center gap-2 rounded-xl bg-teal-600 px-4 py-3 text-sm font-bold text-white disabled:opacity-60">{busy?<Loader2 className="animate-spin" size={17}/>:<LogIn size={17}/>}Masuk</button></form><div className="mt-5 rounded-xl bg-slate-50 p-3 text-[11px] text-slate-500">API: {process.env.NEXT_PUBLIC_API_BASE_URL||'https://api.irkop.workers.dev'}{process.env.NEXT_PUBLIC_API_PREFIX||'/v1/konter'}</div></div></div></div>}
