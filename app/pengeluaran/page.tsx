'use client'; import {ResourceCrud} from '../../components/resource-crud'; import {money,dateID} from '../../lib/format'; export default function Pengeluaran(){return <ResourceCrud title="Pengeluaran" desc="CRUD pengeluaran · data real dari D1" path="/pengeluaran" fields={[{key:'category',label:'Kategori',required:true},{key:'amount',label:'Jumlah',type:'number',required:true},{key:'date',label:'Tanggal',type:'date',required:true},{key:'note',label:'Catatan'}]} columns={[{key:'date',label:'Tanggal',format:v=>dateID(v)},{key:'category',label:'Kategori'},{key:'note',label:'Catatan'},{key:'amount',label:'Jumlah',format:v=>money(v)}]}/> }