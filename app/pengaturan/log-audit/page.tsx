'use client'; import seed from '../../../../lib/seed.json'; import {PageHeader,Toolbar,Card} from '../../../../components/ui';
export default function Audit(){return <><PageHeader title="Log / Audit" desc="Riwayat create, update, delete penting"/><Toolbar searchPlaceholder="Cari tabel, action, detail..."/><Card><div className="overflow-x-auto"><table className="w-full min-w-[780px] text-left text-xs"><thead className="border-b text-slate-500"><tr><th className="pb-3">Waktu</th><th>User</th><th>Action</th><th>Table</th><th>Record</th><th>Detail</th></tr></thead><tbody>{seed.audit_logs.map(a=><tr key={a.id} className="border-b border-slate-50"><td className="py-3">{a.created_at}</td><td>Budi Admin</td><td><span className="rounded-full bg-emerald-50 px-2 py-1 font-semibold text-emerald-700">{a.action}</span></td><td className="font-mono">{a.table_name}</td><td>#{a.record_id}</td><td>{a.detail}</td></tr>)}</tbody></table></div></Card></>}
