'use client'; import seed from '../../lib/seed.json'; import {money} from '../../lib/format'; import {PageHeader,StatCard,Card} from '../../components/ui'; import {Banknote,TrendingUp,ReceiptText,Wallet} from 'lucide-react';
export default function Laporan(){const om=seed.transactions.reduce((a,t)=>a+t.sell_price,0);const lab=seed.transactions.reduce((a,t)=>a+t.profit,0);return <><PageHeader title="Laporan" desc="Analitik transaksi, service HP, dan pengeluaran" action={<div className="flex gap-2"><button className="rounded-xl border bg-white px-3 py-2 text-xs">Excel</button><button className="rounded-xl border bg-white px-3 py-2 text-xs">PDF</button></div>}/><div className="mb-5 flex flex-wrap gap-2"><button className="rounded-xl bg-slate-900 px-4 py-2 text-xs font-semibold text-white">Harian</button><button className="rounded-xl border bg-white px-4 py-2 text-xs font-semibold">Bulanan</button><button className="rounded-xl border bg-white px-4 py-2 text-xs font-semibold">Tahunan</button><button className="rounded-xl border bg-white px-4 py-2 text-xs font-semibold">Custom range</button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Omset" value={money(om)} icon={Banknote}/><StatCard label="Laba Kotor" value={money(lab)} icon={TrendingUp}/><StatCard label="Transaksi" value={String(seed.transactions.length)} icon={ReceiptText}/><StatCard label="Pengeluaran" value={money(seed.expenses.reduce((a,x)=>a+x.amount,0))} icon={Wallet} trend="down"/></div><div className="mt-5 grid gap-4 lg:grid-cols-2"><Card><h2 className="font-bold">Omset per kategori</h2><div className="mt-5 space-y-4">{seed.categories.slice(0,4).map(c=>{const v=seed.transactions.filter(t=>t.category_id===c.id).reduce((a,t)=>a+t.sell_price,0);const pct=om?v/om*100:0;return <div key={c.id}><div className="mb-1 flex justify-between text-xs"><span>{c.name}</span><b>{money(v)}</b></div><div className="h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-teal-500" style={{width:`${Math.max(pct,3)}%`}}/></div></div>})}</div></Card><Card><h2 className="font-bold">Ringkasan service HP</h2><div className="mt-4 space-y-3">{seed.services.map(s=><div key={s.id} className="flex items-center justify-between rounded-xl bg-slate-50 p-3"><div><div className="text-sm font-semibold">{s.device_name}</div><div className="text-xs text-slate-500">{s.damage_description}</div></div><div className="num text-sm font-bold text-emerald-700">{money(s.profit)}</div></div>)}</div></Card></div></>}
