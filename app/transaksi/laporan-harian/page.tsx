'use client'; import seed from '../../../lib/seed.json'; import {money,dateID} from '../../../lib/format'; import {PageHeader,Toolbar,Card,Status} from '../../../components/ui';
export default function LaporanHarian(){return <><PageHeader title="Laporan Transaksi Hari Ini" desc="Daftar transaksi real-time untuk 19 Agustus 2026"/><Toolbar searchPlaceholder="Cari ID, produk, pelanggan..."/><Card><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="border-b border-[var(--line)] text-slate-500"><tr><th className="pb-3">ID</th><th>Produk</th><th>Kategori</th><th>Pelanggan</th><th>Metode</th><th>Jam</th><th className="text-right">Omset</th><th className="text-right">Laba</th></tr></thead><tbody>{seed.transactions.map(t=>{const p=seed.products.find(x=>x.id===t.product_id); const cat=seed.categories.find(x=>x.id===t.category_id);const c=seed.customers.find(x=>x.id===t.customer_id);return <tr key={t.id} className="border-b border-slate-50"><td className="py-3 font-semibold">#{t.id}</td><td>{p?.name}</td><td>{cat?.name}</td><td>{c?.name||'-'}</td><td><Status value="aktif"/> <span className="ml-1">{t.payment_method}</span></td><td>{new Date(t.transaction_date).toLocaleTimeString('id-ID',{hour:'2-digit',minute:'2-digit'})}</td><td className="num text-right font-semibold">{money(t.sell_price)}</td><td className="num text-right font-semibold text-emerald-700">{money(t.profit)}</td></tr>})}</tbody></table></div></Card></>}
